using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

namespace Ar
{
    public enum PlayerInput { MousePosition}

    public enum Scores { addScore};

    public enum Level { start};
}